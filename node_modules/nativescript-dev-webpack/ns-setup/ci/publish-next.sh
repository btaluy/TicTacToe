#!/bin/bash
set -e

package=$1

if [ -z "$2" ];then
tag="next"
else
tag=$2
fi
echo "Publishing ${package}@${tag} ... "  $(date)
echo "PWD: " $PWD
echo "Tag: ${tag}"
cp ./ns-setup/ci/npm/tns-bot/.npmrc .
npm publish ${package} --tag ${tag}
