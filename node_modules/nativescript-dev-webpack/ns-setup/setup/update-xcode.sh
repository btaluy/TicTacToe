#!/bin/bash

if mas outdated | grep -q 'Xcode'; then
  echo "Xcode is outdated!"
  osascript -e 'tell app "Simulator" to quit'
  osascript -e 'tell app "Xcode" to quit'
  mas upgrade $(mas outdated | grep Xcode | cut -d " " -f1)
else
  echo "Xcode is up-to-date!"
fi